
describe("Realm#constructor", () => {
    it("creates a Realm file", () => {
        const realm = new Realm();
        realm.close();
    });

    /*
    it("fails", () => {
        throw new Error("Failed!");
    });
    */
});
