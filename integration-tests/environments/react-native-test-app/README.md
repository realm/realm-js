## Build Realm Core from source

To build iOS with debug symbols, run the `pod-install` NPM script with the environment variable `REALM_BUILD_FROM_SOURCE`.

```bash
REALM_BUILD_FROM_SOURCE=1 npm run pod-install
```
