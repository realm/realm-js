## Build Realm Core from source

To build iOS with debug symbols, run the `pod-install` NPM script with the environment variable `REALM_BUILD_CORE`.

```bash
REALM_BUILD_CORE=1 npm run pod-install
```
