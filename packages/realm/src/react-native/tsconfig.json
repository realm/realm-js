{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "noResolve": false,
    "incremental": true,
    "types": [
      "react-native",
      "path-browserify"
    ]
  },
  "exclude": [
    "../tests/",
    "../node/",
    "../browser/",
    "../../type-tests/"
  ],
}
