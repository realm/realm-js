cmake_minimum_required(VERSION 3.17)
project(RealmJS)

if(ANDROID)
    add_subdirectory(jsi)
    add_subdirectory(android)
elseif(DEFINED CMAKE_JS_VERSION)
    message(FATAL_ERROR "Cannot build the Node.js binding: See ../bindgen for more information.")
elseif(CMAKE_SYSTEM_NAME STREQUAL iOS)
    message(FATAL_ERROR "Cannot build the iOS binding via CMake: We're relying on CocoaPods for this.")
endif()
