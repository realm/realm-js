{
  "name": "@realm/node-tests",
  "version": "0.1.0",
  "description": "Realm JS tests running in a Node.js environment",
  "private": true,
  "scripts": {
    "start": "tsm --no-warnings index.mjs",
    "test": "mocha-remote --reporter @realm/mocha-reporter npm start",
    "lint": "eslint ."
  },
  "dependencies": {
    "@realm/integration-tests": "*",
    "mocha-cli": "^1.0.1",
    "mocha-github-actions-reporter": "^0.2.3",
    "mocha-remote-cli": "^1.5.0",
    "mocha-remote-client": "^1.5.0",
    "realm": "*"
  },
  "devDependencies": {
    "@realm/app-importer": "*",
    "@realm/mocha-reporter": "*",
    "cmake-js": "^6.1.0",
    "concurrently": "^6.0.2",
    "tsm": "^2.3.0"
  }
}
