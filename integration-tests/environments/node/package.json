{
  "name": "realm-node-tests",
  "version": "0.1.0",
  "description": "Realm JS tests running in a Node.js environment",
  "private": true,
  "scripts": {
    "postinstall": "npm install ../../realm-integration-tests.tgz ../../realm.tgz --no-save --build-from-source=realm --realm_enable_sync",
    "update-tests": "npm run tests/pack --prefix ../.. && npm install ../../realm-integration-tests.tgz --no-save",
    "test": "mocha --exit index.js"
  },
  "dependencies": {
    "command-line-args": "^4.0.6",
    "decompress": "^4.2.0",
    "fs-extra": "^7.0.1",
    "https-proxy-agent": "^2.2.3",
    "mocha": "^5.2.0",
    "mocha-cli": "^1.0.1",
    "mocha-junit-reporter": "^1.18.0"
  },
  "peerDependencies": {
    "realm": "*",
    "realm-integration-tests": "*"
  }
}
